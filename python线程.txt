进程的组成：
数据段，代码段，进程控制块(PCB)

进程控制块包括： 
1.进程的标识符pid,name; 
2.处理机的信息(通用寄存器，指令计数器等)
3.进程的调度信息(进程的状态，进程的优先级，进程的关系)
4.进程的控制信息(虚拟地址，资源清单，信号量，互斥量，锁的信息，等)


事务的属性：
1.原子性：一个事务是不可分割的工作单位，事务中的各个操作要么都做要么都不做
2.一致性：事务必须从一个一致性状态到另一个一致性状态
3.隔离性：一个事务的执行不能被其他并发事务干扰
4.持久性：一个事务一旦提交，它对数据库的改变是永久的

mysql中默认Sql语句会自动commit
show variables like "autocommit";
show databases like "mydatabase"

start transaction #开启事务,此时autocommit被禁用，sql执行必须commit才能保存到数据库中

事务回滚只针对表记录的操作：增加，删除，修改。对创建库，创建表示无效的

python提供的mysql模块叫：pymysql
pymysql使用流程：
1.建立数据库连接
2.创建游标对象
3.使用游标对象的方法和sql语句操控mysql数据库
4.提交commit
5.关闭游标
6.关闭数据库连接


进程的三个状态：就绪，执行，等待/阻塞
死锁是怎么回事，怎么解决死锁问题?



from queue import PriorityQueue





















